!/bin/bash

# You can add commands here to run before the lock screen appears.
# For example, to apply a blur to your current wallpaper using imagemagick:
# grim -o /tmp/screenshot.png
# magick /tmp/screenshot.png -blur 0x10 /tmp/screenshot-blurred.png
# Then, run hyprlock
hyprlock
